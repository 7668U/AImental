<block wx:if="{{!isLoggedIn}}">
  <login-prompt 
    title="欢迎来到心灵社区"
    subtitle="请先登录，寻找AI伙伴"
    bind:loginevent="handleLogin"
  ></login-prompt>
</block>

<block wx:else>
  <view class="page-container">
    
    <view class="custom-nav-wrapper" style="padding-top: {{statusBarHeight}}px;">
      <view class="custom-nav" style="height: {{navBarHeight}}px;">
        <view class="nav-left">
          <image class="action-icon" src="/images/icons/new_item.png" bindtap="addMoreFriends"></image>
        </view>
        <view class="nav-center">
          <text class="nav-title">心灵社区</text>
        </view>
        <view class="nav-right"></view>
      </view>
    </view>

    <scroll-view 
      class="list-container" 
      scroll-y="true"
      style="padding-top: {{totalHeaderHeight}}px;"
    >
      <block wx:if="{{chatList.length > 0}}">
        <view class="chat-item" wx:for="{{chatList}}" wx:key="id" bindtap="navigateToChat" data-ai="{{item}}">
          <view class="avatar-container">
            <image class="avatar" src="{{item.avatar}}"></image>
            <view class="unread-dot" wx:if="{{item.unread}}"></view>
          </view>
          <view class="info-container">
            <view class="info-header">
              <text class="name">{{item.name}}</text>
              <text class="time">{{item.time}}</text>
            </view>
            <view class="last-message">
              <text>{{item.lastMessage}}</text>
            </view>
          </view>
        </view>
      </block>
      
      <block wx:else>
        <view class="empty-state">
          <image class="empty-icon" src="/images/icons/empty-chat.png"></image>
          <text class="empty-text">还没有好友，快点击加号寻找新的伙伴吧</text>
        </view>
      </block>
    </scroll-view>

  </view>
</block>